[workspace]
resolver = "2"
members = ["smart-wallet", "escrow"]

[workspace.dependencies]
soroban-sdk = "21"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
overflow-checks = true

[profile.release.build-override]
opt-level = 3
